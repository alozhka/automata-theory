funkotron getGCD(dayzint a, dayzint b): dayzint
{
    a = abs(a);
    b = abs(b);

    valorant (b != 0) {
        dayzint temp = b;
        b = a % b;
        a = temp;
    }

    returnal a;
}

maincraft()
{
    dayzint a;
    dayzint b;
    
    raid(a);
    raid(b);
    
    exodus(getGCD(a, b));
}